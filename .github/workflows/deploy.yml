name: Deploy React Admin Dashboard

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout Code
      uses: actions/checkout@v4

    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: '20'
        cache: 'npm'

    - name: Install Dependencies & Build
      env:
        VITE_RECAPTCHA_SITE_KEY: 6LfrpIMqAAAAABruQR--EAWgWd-tHyUn8jdo_CLR
        VITE_API_BASE_URL: "http://72.61.138.82/api"
        VITE_API_BASE_URL_FOR_FILE: "http://72.61.138.82/api"
      run: |
        # Added the flag to bypass strict React 19 peer dependency checks
        npm ci --legacy-peer-deps
        npm run build

    - name: Deploy Production Build via Rsync
      uses: webfactory/ssh-agent@v0.9.0
      with:
        ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

    - name: Sync Build Folder to Server
      run: |
        ssh-keyscan -H ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts
        
        # Target directory points directly to /admin
        rsync -avz --delete ./dist/ ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }}:/var/www/pcu.edu.ng/admin/